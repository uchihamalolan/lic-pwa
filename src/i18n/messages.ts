import { type Catalog, type MessagesByLocale } from "@astryxdesign/core/i18n";

const en: Catalog = {
  "@lic.actions.save": { defaultMessage: "Save" },
};

export const messages: MessagesByLocale = { en };
